/**
 * Firebase has been replaced by the local FastAPI + SQLite backend.
 * This file is kept as a stub so any leftover imports don't crash at build time.
 * All auth and data operations now go through src/lib/api.js.
 */
export const auth    = null;
export const db      = null;
export const storage = null;
export default null;
